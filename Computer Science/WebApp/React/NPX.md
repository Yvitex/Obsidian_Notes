# Npx 
Is a specific command living inside the [[NPM]]. 

If [[NPM]] manages to install, locate and delete specific modules using 3 separate commands, NPX could do it using a single command, so when we saw
```shell
$ npx cowsaw hello
```
![[Pasted image 20220723054316.png]]

It will automatically executes and delete at the same time
![[Pasted image 20220723054353.png]]

